<!DOCTYPE html>
<html>
  <head>
    <meta charset='utf-8'>
    <meta name="viewport" content="width=device-width, initial-scale=1"> 
    <%= stylesheet_link_tag 'sites_manifest' %> 
    <%= javascript_include_tag 'sites_manifest' %>
    <%= csrf_meta_tags %>
    <%= google_analytics_js 'UA-37983247-1' %>
  </head>
  <body>
    <div id='home' data-role='page' data-theme='<%= @site.theme %>'>
      <div data-role='content'>
        <%= yield :logo_img %>
        <%= yield :action_buttons %>
        <%= yield :nav_menu %>
        <%= yield %>
        <div id='md_theme' data-mercury='full' style='display:none'><%= @site.theme %></div>
        <a href="<%= @site.url + '?no_redirect=1' %>" data-role='button' data-inline='false' data-icon='back'>Go to full site</a>
      </div>
      <% if true %>
        <div data-role="popup" id='switch_to_classic' class='ui-content' data-theme='e' data-overlay-theme='a'>
          <a href="#" data-rel="back" data-role="button" data-theme="a" data-icon="delete" data-iconpos="notext" class="ui-btn-left">Close</a>
          <p>Welcome to <%= @site.subdomain + '.com' %>'s mobile website. Tap <a href="<%= @site.url + '?no_redirect=1' %>">here</a> or the button at the end of this page to go to the full site. Thanks!</p>
        </div>
      <% end %>
    </div>
  </body>
</html>
